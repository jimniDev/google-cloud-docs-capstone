---
description: Nimbus Design System component patterns for React TSX files. Enforces layout structure, component variants, interaction states, accessibility, and icon usage.
globs: src/app/components/**/*.tsx
alwaysApply: false
---

# Nimbus Component Patterns

## Layout: Docs Layout (default for this project)

```tsx
// Sidebar: w-[240px], sticky, left
// Content: max-w-[760px], px-nimbus-8
// TOC (optional): w-[200px], right, sticky

<div className="flex min-h-screen bg-nimbus-surface">
  <aside className="w-[240px] shrink-0 sticky top-0 h-screen border-r border-nimbus-border bg-nimbus-surface-subtle" />
  <main className="flex-1 max-w-[760px] px-nimbus-8 py-nimbus-6" />
  <nav className="w-[200px] shrink-0 sticky top-0 h-screen" /> {/* optional TOC */}
</div>
```

Top bar height: `h-[56px]`. Sidebar width: `w-[240px]`.

## Buttons

```tsx
// Primary
<button className="min-w-[96px] h-[36px] px-nimbus-4 rounded-nimbus-md
  bg-nimbus-primary text-white font-medium text-[14px]
  hover:bg-nimbus-primary-hover active:bg-nimbus-primary-active
  focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-nimbus-primary focus-visible:ring-offset-2
  disabled:opacity-40 disabled:pointer-events-none
  transition-[background-color,box-shadow] duration-[120ms] ease-out">
  Label
</button>

// Secondary (Ghost)
<button className="min-w-[96px] h-[36px] px-nimbus-4 rounded-nimbus-md
  border border-nimbus-border text-nimbus-text-primary
  hover:bg-nimbus-surface-subtle
  focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-nimbus-primary focus-visible:ring-offset-2
  disabled:opacity-40 disabled:pointer-events-none">
  Label
</button>

// Destructive
<button className="... bg-nimbus-error text-white hover:opacity-90">Delete</button>
```

## Cards

```tsx
// Default
<div className="rounded-nimbus-lg border border-nimbus-border bg-nimbus-surface shadow-nimbus-sm p-nimbus-4">

// Elevated
<div className="rounded-nimbus-lg bg-nimbus-surface shadow-nimbus-md p-nimbus-6">
```

Card title spacing: `mb-nimbus-3`. Body text: `text-nimbus-text-secondary`.

## Alerts

```tsx
// success | warning | error | info
const alertStyles = {
  success: 'border-nimbus-success bg-[#E6F4EA] text-nimbus-success',
  warning: 'border-nimbus-warning bg-[#FEF7E0] text-[#B06000]',
  error:   'border-nimbus-error bg-[#FCE8E6] text-nimbus-error',
  info:    'border-nimbus-info bg-[#E3F2FD] text-nimbus-info',
};

<div className={`flex gap-nimbus-2 rounded-nimbus-md border p-nimbus-3 ${alertStyles[variant]}`}>
  <span className="material-symbols-outlined text-[20px]">{icon}</span>
  <div>
    <p className="font-medium">{title}</p>
    <p className="text-[14px] mt-nimbus-1">{description}</p>
  </div>
</div>
```

## Focus & Interaction States

Always include focus-visible ring. Never remove outlines without replacement.

```tsx
// Focus ring pattern (apply to all interactive elements)
"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-nimbus-primary focus-visible:ring-offset-2"

// Disabled pattern
"disabled:opacity-40 disabled:pointer-events-none"

// Hover/Active for surfaces
"hover:bg-nimbus-surface-subtle active:bg-nimbus-border"
```

## Icons (Material Symbols)

Use the Outlined style. Load via Google Fonts in `fonts.css`:

```html
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />
```

```tsx
// Sizes: 16px (sm), 20px (default), 24px (lg), 32px (hero)
<span className="material-symbols-outlined text-[20px] text-nimbus-text-secondary">search</span>

// Icon + label: gap-nimbus-2
<button className="flex items-center gap-nimbus-2">
  <span className="material-symbols-outlined text-[20px]">add</span>
  New item
</button>

// Icon-only button: 36×36 container
<button className="w-[36px] h-[36px] flex items-center justify-center rounded-nimbus-md">
  <span className="material-symbols-outlined text-[20px]">more_vert</span>
</button>
```

Do not mix Outlined and Rounded/Sharp styles in the same view.

## Motion

```tsx
// Page transitions: fade + slide 8px, motion-medium
"transition-[opacity,transform] duration-[180ms] ease-[ease]"

// Component transitions: motion-fast
"transition-[background-color,box-shadow] duration-[120ms] ease-out"

// Chip press: scale-[0.98]
// Dropdown: translate-y-2 + opacity-0 → translate-y-0 + opacity-100
```

## Accessibility Checklist (WCAG 2.2 AA)

- Text contrast ≥ 4.5:1 (`text-nimbus-text-primary` on `bg-nimbus-surface` is compliant)
- Interactive elements contrast ≥ 3:1
- All interactive elements keyboard-focusable with visible focus ring
- Icon-only buttons have `aria-label`
- Form inputs have associated `<label>` or `aria-label`
